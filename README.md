# DES Round Tracer – PHP Implementation

این پروژه یک پیاده‌سازی کامل از الگوریتم **DES** به زبان PHP است که با هدف **تحلیل اثر بهمنی (Avalanche Effect)** طراحی شده است. در این نسخه، تغییر یک بیت در ورودی (plaintext) یا در کلید (key) به صورت خودکار انجام می‌شود و خروجی هر راند و تغییرات حاصل در قالب فایل‌های CSV ذخیره می‌شود.

---

## 🎯 هدف پروژه
هدف اصلی این تحلیل، مشاهده و اندازه‌گیری میزان تغییرات بیت‌ها در هر راند DES است تا نشان داده شود چرا **وجود ۱۶ راند** ضروری است و چرا راندهای میانی به‌تنهایی برای ایجاد پدیده بهمنی کافی نیستند.

---

## ⚙️ ویژگی‌ها
- پیاده‌سازی کامل الگوریتم **DES** بدون نیاز به افزونه یا کتابخانه خارجی
- ذخیره‌ی خروجی هر راند در فایل `per_round_outputs.csv`
- ذخیره‌ی خلاصه‌ی هر اجرا (تعداد بیت‌های تغییر کرده و زمان اجرا) در `run_summary.csv`
- تحلیل فاصله‌ی همینگ (Hamming Distance) بین خروجی‌های متوالی در `analysis_by_round.csv`
- ذخیره‌ی خروجی‌ها به دو صورت:
  - **Hexadecimal** برای نمایش خروجی نهایی DES
  - **L و R جداگانه** برای بررسی دقیق نیمه‌های چپ و راست
- امکان تست در دو حالت:
  - **Flip در plaintext** (تغییر بیت‌های ورودی)
  - **Flip در key** (تحلیل حساسیت کلید)
- قابلیت انتخاب بازهٔ بیت‌ها برای flip (مثلاً 23 تا 64)
- اندازه‌گیری زمان اجرای هر run با `microtime(true)`

---

## 🧮 ساختار خروجی‌ها
تمام خروجی‌ها در پوشه‌ی `des_outputs/` ذخیره می‌شوند:

| فایل | توضیح |
|------|--------|
| `per_round_outputs.csv` | خروجی هر راند برای هر اجرای تست |
| `run_summary.csv` | زمان و نتیجهٔ نهایی هر run |
| `analysis_by_round.csv` | میانگین تغییرات بیت‌ها در هر راند |
| `final_hamming_distribution.csv` | توزیع تعداد بیت‌های تغییر کرده بین کل خروجی‌ها |

---

## 🚀 نحوه اجرا
1. فایل `des_round_tracer_full.php` را در سرور یا محیط PHP خود قرار دهید.
2. مطمئن شوید که PHP دارای مجوز نوشتن در پوشهٔ پروژه است.
3. اجرا کنید:
   ```bash
   php des_round_tracer_full.php
   ```
4. پس از اتمام اجرا، پوشهٔ `des_outputs/` شامل فایل‌های خروجی و تحلیل خواهد بود.

---

## 🧩 پارامترهای قابل تنظیم در کد
در ابتدای فایل `des_round_tracer_full.php` می‌توانید مقادیر زیر را تغییر دهید:
```php
$flip_mode = 'plaintext'; // یا 'key'
$runs = 100;               // تعداد اجراها
$bit_low = 23;             // کمترین بیت برای flip
$bit_high = 64;            // بیشترین بیت برای flip
```

---

## 🧠 چرا فقط ۱۶ راند؟ چرا راندهای وسط کافی نیستند؟

### توضیح ساده
DES طوری طراحی شده که تغییر یک بیت در ورودی، پس از چند راند، باید تقریباً نیمی از بیت‌های خروجی را تغییر دهد. این خاصیت را «اثر بهمنی» می‌نامند. در راندهای اولیه، تغییرات هنوز به‌طور کامل منتشر نشده‌اند؛ در نتیجه خروجی به تغییرات محلی حساس است و امنیت پایین است. در راندهای آخر، وابستگی بین بیت‌های کلید و داده به حداکثر می‌رسد. بر اساس آزمایش‌های تجربی، ۱۶ راند نقطه‌ای است که دیگر افزودن راند جدید تغییر محسوسی در پراکندگی بیت‌ها ایجاد نمی‌کند.

### توضیح فنی
در هر راند DES، خروجی نیمهٔ راست وارد تابع `F` می‌شود که با زیرکلید ترکیب شده و سپس با نیمهٔ چپ XOR می‌شود. از آن‌جا که `F` شامل جایگشت‌ها و S-boxهاست، اثر تغییر یک بیت به‌صورت غیرخطی در تمام خروجی منتشر می‌شود. اما این پدیده به زمان نیاز دارد:

- در راندهای ۱ تا ۳: تغییر تنها چند بیت از خروجی را تحت تأثیر قرار می‌دهد.
- در راندهای ۴ تا ۷: توزیع تغییرات بین نیمه‌ها شروع به گسترش می‌کند.
- در راندهای ۸ تا ۱۲: بیش از نیمی از بیت‌ها تغییر می‌کنند، اثر بهمنی کامل‌تر می‌شود.
- در راندهای ۱۳ تا ۱۶: سیستم به حالت اشباع رسیده و هر تغییر کوچک تقریباً تمام خروجی را متأثر می‌کند.

اگر DES تعداد راند کمتری داشت (مثلاً ۸ یا ۱۰)، تحلیل‌های تفاضلی و خطی می‌توانستند با پیچیدگی محاسباتی بسیار پایین‌تر کلید را تخمین بزنند.

---

## ⏱️ تحلیل سرعت
برای هر run، زمان آغاز و پایان با `microtime(true)` اندازه‌گیری و در `run_summary.csv` ثبت می‌شود. برای تست‌های بزرگ (مثلاً ۱۰۰۰+ اجرا)، می‌توانید میانگین زمان را محاسبه کرده و از آن برای تحلیل کارایی DES استفاده کنید.

---

## 💡 پیشنهادهای توسعه آینده
- افزودن حالت‌های CBC و ECB برای مقایسه‌ی انتشار خطا
- تحلیل Hamming distance بین کلیدها و خروجی نهایی
- نمایش گرافیکی تغییرات هر راند با Chart.js یا Plotly (در نسخه وب)
- فشرده‌سازی خودکار خروجی‌ها در پایان اجرا

---

## 📜 مجوز و انتشار
پروژه تحت مجوز MIT منتشر می‌شود و می‌توانید آن را در گیت‌هاب خود قرار دهید.
نمونه نام پیشنهادی مخزن:
```
DES-Round-Tracer-PHP
```

ساخته شده با ❤️ برای تحلیل و آموزش مفاهیم رمزنگاری کلاسیک.
